import 'package:flutter/material.dart';

class AccessMakeApp extends StatelessWidget {
  const AccessMakeApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp();
  }
}
